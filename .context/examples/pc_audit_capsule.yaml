# Example: PrivateConsumer audit sitter InvestigationCapsule
# Represents a gate failure investigation for a transcode quality gate.
# All paths, ids, and values are illustrative — not tied to private config.

capsule_id: "inv-20260521-transcode-quality-001"
schema_version: "0.1"
created_at: "2026-05-21T14:32:00Z"
updated_at: "2026-05-21T15:10:00Z"
created_by: "audit-sitter-worker-03"
parent_capsule_id: ""
related_checkpoint_id: "chk-20260521-1430-pc-audit"
status: "active"

current_blocker: "transcode quality gate failing on job batch-20260521-a: score 0.61, threshold 0.85"
current_phase: "remediation"
failing_invariant: "transcode_quality_gate — batch-20260521-a — score 0.61 < threshold 0.85"

active_hypotheses:
  - "CRF value too high for source material bitrate"
  - "source file has encoding anomalies causing metric degradation"
  - "quality metric implementation regression in last deploy"

evidence_paths:
  - ".console/audits/2026-05-21/batch-20260521-a/gate_results.json"
  - ".console/audits/2026-05-21/batch-20260521-a/transcode_log.txt"
  - ".context/capsules/inv-20260520-transcode-quality-003.yaml"

recent_failures:
  - timestamp: "2026-05-21T14:30:00Z"
    description: "batch-20260521-a transcode quality gate failed"
    artifact_path: ".console/audits/2026-05-21/batch-20260521-a/gate_results.json"
  - timestamp: "2026-05-21T12:15:00Z"
    description: "batch-20260521-b transcode quality gate failed (same threshold)"
    artifact_path: ".console/audits/2026-05-21/batch-20260521-b/gate_results.json"

attempted_remediations:
  - action: "rerun transcode with default CRF"
    outcome: "no improvement — score 0.63"
    timestamp: "2026-05-21T14:50:00Z"

next_actions:
  - "inspect transcode_log.txt for encoder warning lines"
  - "compare CRF settings between passing and failing batches"
  - "check if quality metric implementation changed in recent commits"
  - "if source anomaly confirmed: mark batch for manual review and update capsule status to blocked"

safety_constraints:
  - "do not delete any audit artifacts"
  - "do not restart the transcode pipeline — rerun single jobs only"
  - "do not modify gate threshold values"

known_safe_boundaries:
  - "re-reading .console/audits/ is safe"
  - "re-running validation scripts against existing artifacts is safe"
  - "updating this capsule file is safe"

exclusions:
  reload_forbidden:
    - ".context/archive/ — stale capsules, superseded by current investigation"
    - ".console/audits/ older than 2026-05-20 — not relevant to current batch"
  retry_forbidden:
    - "manual ffmpeg parameter override — caused data corruption on batch-20260519-c"

handoff_notes: >
  Focus on hypothesis 1 (CRF value) first — the log comparison in hypothesis 3 can
  wait until CRF is ruled out. Do not reload the full audit history.
  Check .console/audits/2026-05-21/ only.
